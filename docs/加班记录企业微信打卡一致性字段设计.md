# 加班记录新增"是否与企业微信打卡时间一致"字段设计文档

## 1. 需求背景

在加班记录管理中，需要增加一个字段用于记录"是否与企业微信打卡时间一致"，以便核实加班申请的真实性。

## 2. 字段设计

| 属性 | 值 |
|------|-----|
| 字段名 | `weworkClockMatchFlag` |
| 数据库字段名 | `wework_clock_match_flag` |
| 字段含义 | 是否与企业微信打卡时间一致 |
| 数据类型 | `Integer` / `TINYINT(1)` |
| 取值范围 | 0=否，1=是 |
| 是否必填 | 是 |
| 默认值 | 无（必填，历史数据设为0） |

### 2.1 命名说明

- 字段命名遵循现有 `xxxFlag` 规范（参考 `overtimeFlag`、`doneFlag`、`archiveFlag`）
- `wework` = 企业微信（WeChat Work/WeCom）
- `clock` = 打卡
- `match` = 一致/匹配
- `flag` = 标志位

## 3. 改动清单

| 序号 | 层级 | 文件 | 改动说明 |
|------|------|------|----------|
| 1 | 数据库 | `biz_overtime_record` 表 | 新增 `wework_clock_match_flag` 字段 |
| 2 | 实体 | `BizOvertimeRecord.java` | 新增 `weworkClockMatchFlag` 字段及注解 |
| 3 | Controller | `OvertimeRecordController.java` | add/edit 方法增加必填校验 |
| 4 | SQL脚本 | `docs/sql/overtime_record_wework_flag.sql` | 数据库变更脚本 |

## 4. 详细改动

### 4.1 数据库变更

**文件：** `docs/sql/overtime_record_wework_flag.sql`

```sql
-- 1. 新增字段（允许 NULL，以便历史数据处理）
ALTER TABLE biz_overtime_record
ADD COLUMN wework_clock_match_flag TINYINT(1) COMMENT '是否与企业微信打卡时间一致，0=否，1=是';

-- 2. 历史数据设置默认值（设置为 0=否）
UPDATE biz_overtime_record
SET wework_clock_match_flag = 0
WHERE wework_clock_match_flag IS NULL;

-- 3. 修改为 NOT NULL（新数据必填）
ALTER TABLE biz_overtime_record
MODIFY COLUMN wework_clock_match_flag TINYINT(1) NOT NULL COMMENT '是否与企业微信打卡时间一致，0=否，1=是';
```

### 4.2 实体类改动

**文件：** `jeecg-module-system/jeecg-system-biz/src/main/java/org/jeecg/modules/mzx/entity/BizOvertimeRecord.java`

**新增字段：**

```java
/**
 * 是否与企业微信打卡时间一致，0=否，1=是
 */
@Excel(name = "是否与企微打卡一致", width = 15, dicCode = "yn")
@Dict(dicCode = "yn")
private Integer weworkClockMatchFlag;
```

**说明：**
- 使用 `@Dict(dicCode = "yn")` 注解，自动关联系统字典（是/否）
- 使用 `@Excel` 注解支持 Excel 导入导出

### 4.3 Controller 改动

**文件：** `OvertimeRecordController.java`

**add 方法改动：**

```java
// 校验必填
if (overtimeRecord.getWeworkClockMatchFlag() == null) {
    throw new JeecgBootException("是否与企业微信打卡时间一致为必填项");
}
```

**edit 方法改动：**

在设置其他字段之前增加校验：

```java
// 校验必填
if (overtimeRecord.getWeworkClockMatchFlag() == null) {
    throw new JeecgBootException("是否与企业微信打卡时间一致为必填项");
}

// 更新字段
data.setWeworkClockMatchFlag(overtimeRecord.getWeworkClockMatchFlag());
```

## 5. 实施顺序

1. 执行数据库变更 SQL
2. 修改实体类，新增字段
3. 修改 Controller，增加必填校验
4. 前端改动（表单新增字段，列表展示）
5. 测试验证

## 6. 前端改动说明

### 6.1 表单改动

在加班申请表单中新增字段：

```vue
<a-form-item label="是否与企微打卡一致" :labelCol="labelCol" :wrapperCol="wrapperCol">
  <j-dict-select-tag v-model="model.weworkClockMatchFlag" dictCode="yn" placeholder="请选择" />
</a-form-item>
```

### 6.2 列表改动

在加班记录列表中新增展示列：

```vue
<a-table-column title="是否与企微打卡一致" dataIndex="weworkClockMatchFlag_dictText" width="120px" />
```

### 6.3 校验规则

前端表单校验规则：

```javascript
rules: {
  weworkClockMatchFlag: [{ required: true, message: '请选择是否与企业微信打卡时间一致' }]
}
```

## 7. 风险评估

| 风险点 | 说明 | 应对措施 |
|--------|------|----------|
| 历史数据 | 新字段设为 NOT NULL，历史数据需处理 | 执行 SQL 时先 ADD COLUMN 允许 NULL，再 UPDATE 设置默认值，最后 MODIFY 为 NOT NULL |
| 前端兼容 | 新字段必填，旧数据编辑时需处理 | 编辑时回显已有值，无值时提示必填 |

## 8. 验证方案

1. **后端验证**：
   - 启动应用，访问 Swagger 文档 `http://localhost:8081/xcom/doc.html`
   - 测试 `/overtimeRecord/add` 接口，验证字段必填校验
   - 测试 `/overtimeRecord/edit` 接口，验证字段更新
   - 测试 `/overtimeRecord/list` 接口，验证字段返回

2. **数据库验证**：
   - 检查字段是否成功添加：`DESC biz_overtime_record;`
   - 检查历史数据是否正确设置默认值

---

**文档版本：** v1.0
**创建时间：** 2026-07-04
**创建人：** AI Assistant
